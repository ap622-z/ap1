# 技术选型

- 项目技术栈的唯一事实源；随迭代就地更新、不按版本复制。

## 中间件

- **qdrant**：向量数据库，承载偶像知识/歌词向量
- **mysql**：关系数据库，业务数据事实源
- **redis**：缓存与限流
- **nginx**：反向代理 / 门控
- **rabbitmq**：消息队列（异步投递）

## 运行容器

- **docker**：全部依赖与项目运行/打包方式

> 当前启用 mysql / redis / qdrant；nginx、rabbitmq 为目标架构引入。
