# AP1 Context

面向粉丝的"偶像 Agent"情感陪伴系统：单一偶像人格作为全局单例服务所有用户，自身不持有任何用户状态。多用户的数据彼此隔离，靠"一切状态外置、一切动作归属于某个 Scope"来保证；本词汇表为承载这一规则的五个核心概念给出唯一含义。

## Language

**Scope**:
每个动作与每条数据都归属于某个 Scope——即**某个用户在某个会话中的某次运行**，外加其所处的环境。Scope 是数据隔离的最小单元：状态与动作只在所属 Scope 内存在和可见，越过 Scope 的访问在概念上就不成立。
_Avoid_: token、登录态、请求上下文

**Repository**:
持久状态的唯一主人。系统内一切被保存下来的东西（用户消息、回复、工具记录、偶像知识）都由它拥有和管理；其他组件——尤其是 Agent——不直接持有任何状态。Agent 之所以能零状态运行，正因为所有读写都收敛到这一个入口。
_Avoid_: dao、db、数据层

**Worker**:
一条消息从"被收到"到"回复就绪"之间全部后端处理的执行者：它接走消息、驱动 Agent 产出回复、并把结果保存下来。Agent 只负责思考与产出，Worker 负责其余一切，二者由此解耦。
_Avoid_: service、handler、controller

**Mailbox**:
"同一会话同一时刻至多处理一条消息"这一约定的载体：一个会话的消息逐个、按到达顺序被处理，永不并发交错。它是会话之间的防串扰边界。
_Avoid_: lock、queue、信号量

**Outbox**:
一条可靠交付约定：当一次状态改变必然引发后续处理时，把"该发生什么"与这次改变**一同、原子地**记成既定事实，使后续处理在系统中途失败后仍会发生。它描述的是这个承诺本身，而非任何具体通道。
_Avoid_: MQ、event bus、消息队列
